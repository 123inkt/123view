{% extends 'base.html.twig' %}

{% block page %}

    <header class="navbar navbar-expand-lg navbar-dark bg-dark">
        <a class="navbar-brand mr-0 mr-md-2 header-nav-brand" href="{{ path('DR\\GitCommitNotification\\Controller\\App\\RulesController') }}"
           aria-label="Git commit notification" title="Git commit notification">
            G
        </a>
        <a class="navbar-brand" href="{{ path('DR\\GitCommitNotification\\Controller\\App\\RulesController') }}">it Commit Notification</a>


        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav">
                <li class="nav-item">
                    <a class="nav-link" aria-label="{{ 'Home'|trans }}" title="{{ 'Home'|trans }}"
                       href="{{ path('DR\\GitCommitNotification\\Controller\\App\\RulesController') }}">
                        <i class="bi-house"></i>
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" aria-label="{{ 'Logout'|trans }}" title="{{ 'Logout'|trans }}"
                       href="{{ path('DR\\GitCommitNotification\\Controller\\Auth\\LogoutController') }}">
                        <i class="bi-box-arrow-right"></i>
                    </a>
                </li>
            </ul>
        </div>
    </header>

    <div class="container-xl">
        {% block page_content %}{% endblock %}
    </div>

{% endblock %}
