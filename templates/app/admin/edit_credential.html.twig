{% extends 'app/app.base.html.twig' %}

{% block page_content %}
    <h1 class="mb-4 mt-2">
        {% if editCredentialModel.webhook.id is same as null %}
            {{ 'add.credential'|trans }}
        {% else %}
            {{ 'edit.credential'|trans }} #{{ editCredentialModel.webhook.id }}
        {% endif %}
    </h1>

    {{ form(editCredentialModel.form ) }}

{% endblock %}
